package tdk_enum.factories.solver_factory;

import tdk_enum.ml.solvers.ISolver;

public class SolverFactory implements ISolverFactory {
    @Override
    public ISolver produce() {
        return null;
    }
}
